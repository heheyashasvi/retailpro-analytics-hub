'use client'

// Component to handle automatic cache management
export function CacheManager() {
  // This component doesn't render anything and doesn't set up automatic cache management
  // to avoid circular dependencies and module loading issues
  return null
}